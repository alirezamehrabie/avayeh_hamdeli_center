# مستند جامع فنی و تحلیلی سامانه مرکز آوای همدلی

## مشخصات سند

| عنوان | مقدار |
|---|---|
| نام پروژه | سامانه مرکز آوای همدلی |
| نوع سامانه | سامانه اطلاعاتی تحت وب، فول‌استک و سرورمحور |
| حوزه کاربرد | مدیریت مددجویان، خانوارها، مددکاران اجتماعی، خدمات حمایتی، توزیع اقلام، فعالیت‌ها، حامیان کودک و پرونده مددجو |
| چارچوب اصلی | Laravel 11 و Livewire 3 |
| زبان اصلی پیاده‌سازی | PHP 8.2 یا جدیدتر |
| زبان رابط کاربری | فارسی، راست‌به‌چپ |
| تاریخ تحلیل مخزن | ۲۶ تیر ۱۴۰۵، برابر با ۱۷ ژوئیه ۲۰۲۶ |
| قالب سند | Markdown، مناسب انتقال و ویرایش در Microsoft Word |
| مبنای تحلیل | ساختار مخزن، کد منبع، migrationها، مدل‌ها، مسیرها، آزمون‌ها، فایل‌های پیکربندی و وابستگی‌ها |

> **دامنه و محدودیت تحلیل:** این سند از روی نسخه موجود کد در تاریخ فوق تهیه شده است. هرجا رفتار عملیاتی، تصمیم سازمانی یا زیرساخت استقرار از روی کد به‌طور قطعی قابل استنتاج نبوده، موضوع با عبارات «نیازمند تأیید» یا «در کد به‌صورت صریح مشخص نشده است» علامت‌گذاری شده است. مقادیر محرمانه فایل‌های محیطی در این سند بازنشر نشده‌اند.

---

## ۱. معرفی کلی پروژه

سامانه مرکز آوای همدلی یک نرم‌افزار یکپارچه تحت وب برای ثبت، نگهداری، پردازش و گزارش‌گیری از اطلاعات عملیاتی یک مرکز خیریه یا حمایتی است. هسته دامنه سامانه بر «مددجو» و «خانوار» استوار است، اما کارکرد آن از یک دفتر ثبت اطلاعات فراتر می‌رود و فرایندهای مرتبط با مددکاران اجتماعی، تعریف و تخصیص خدمات، کنترل توزیع در چند ایستگاه، برگزاری فعالیت‌ها، ثبت حضور، مدیریت حامیان کودک، پرونده زمانی مددجو و گزارش‌های مدیریتی را نیز در یک بستر مشترک پوشش می‌دهد.

پروژه از نظر فنی یک سامانه **فول‌استک سرورمحور** است. منطق کسب‌وکار، احراز هویت، مجوزدهی، پردازش داده و دسترسی به پایگاه داده در Laravel اجرا می‌شود. رابط کاربری با Blade و Livewire ساخته شده و برای تعاملات سبک مرورگر از Alpine.js و چند ماژول JavaScript استفاده می‌شود. بنابراین پروژه یک برنامه تک‌صفحه‌ای مستقل به معنای متداول React یا Vue نیست؛ وضعیت اصلی صفحات و عملیات‌ها در مؤلفه‌های Livewire سمت سرور نگهداری می‌شود و Alpine.js بیشتر مسئول وضعیت محلی رابط، نمایش پنجره‌ها، انتخاب دوربین، ورودی‌های تاریخ و مبلغ و بازخوردهای سریع است.

مخزن بررسی‌شده شامل حدود ۱٬۵۰۶ فایل نسخه‌پذیر است. ۱۱۹ فایل migration، ۱۱۸ قالب Blade و ۵۰ فایل در ساختار آزمون‌ها وجود دارد. شمار بالای فایل‌های PHP تا حد زیادی ناشی از نگهداری کپی داخلی برخی کتابخانه‌های کمکی، به‌ویژه Carbon، Assert و ابزار تاریخ جلالی، در پوشه `app/Helpers` است و نباید آن را صرفاً معادل بزرگی منطق اختصاصی سامانه تلقی کرد.

## ۲. هدف و ضرورت پروژه

هدف اصلی پروژه، ایجاد یک منبع داده متمرکز و قابل اتکا برای عملیات روزمره مرکز آوای همدلی است. در محیط‌های حمایتی، اطلاعات معمولاً میان فرم‌های کاغذی، فایل‌های صفحه‌گسترده، پیام‌رسان‌ها و حافظه افراد توزیع می‌شود. این پراکندگی پیامدهایی مانند ثبت تکراری، نبود تاریخچه معتبر، دشواری در کنترل سهمیه‌ها، ضعف گزارش‌گیری و افزایش احتمال خطای انسانی دارد.

سامانه حاضر می‌کوشد ضرورت‌های زیر را پاسخ دهد:

1. ایجاد پرونده جامع و ساخت‌یافته برای هر مددجو و خانوار؛
2. جلوگیری از دوباره‌کاری و ناسازگاری اطلاعات هویتی، خانوادگی و حمایتی؛
3. امکان تخصیص روشن خانوارها به مددکاران اجتماعی؛
4. تعریف خدمات به‌صورت برنامه یا کمپین و کنترل موجودی، دسته‌بندی و سهمیه؛
5. ثبت قابل ردیابی تحویل خدمت به فرد یا خانوار؛
6. کنترل فرایند توزیع در ورودی، محل تحویل و خروج؛
7. ثبت حضور مددجویان در فعالیت‌ها با QR یا روش دستی؛
8. نگهداری تاریخچه اقدامات، پیگیری‌ها، یادداشت‌ها و اسناد پرونده؛
9. فراهم‌کردن گزارش‌های عملیاتی و مدیریتی با فیلترهای قابل ذخیره؛
10. اعمال تفکیک وظایف و کنترل دسترسی بر اساس نقش و مجوز.

## ۳. مسئله‌ای که سامانه حل می‌کند

مسئله محوری پروژه را می‌توان «مدیریت چرخه حمایت از مددجو با حفظ یکپارچگی داده و قابلیت حسابرسی» تعریف کرد. این مسئله از چند زیرمسئله تشکیل شده است:

- اطلاعات مددجو فقط اطلاعات هویتی نیست و باید وضعیت خانوادگی، سرپرست، محل سکونت، تماس، بانک، تحصیل، مهارت، آسیب، معلولیت، سطح نیاز و پوشش حمایتی را نیز دربرگیرد.
- برخی خدمات به شخص و برخی به کل خانوار تعلق می‌گیرند؛ بنابراین گیرنده خدمت همیشه یک نوع موجودیت ثابت نیست.
- خدمت ممکن است در منزل، در یک فعالیت یا در ایستگاه توزیع تحویل شود و هر کانال قواعد عملیاتی متفاوتی دارد.
- یک خدمت می‌تواند چند قلم یا دسته داشته باشد و برای هر دسته، موجودی و سهمیه مددکار مستقل تعریف شود.
- عملیات توزیع هم‌زمان مستعد دوبارکلیک، اجرای موازی در چند تب، ثبت تکراری و عبور از موجودی است.
- فعالیت‌ها ظرفیت، وضعیت چرخه عمر، اپراتور مجاز و حضور یکتای هر مددجو دارند.
- داده‌های مددجویان حساس‌اند و دسترسی به آن‌ها باید بر اساس نقش، مجوز و زمینه عملیاتی محدود شود.
- مدیران به گزارش‌های ترکیبی نیاز دارند، اما ساخت گزارش نباید وابسته به تغییر کد برای هر پرسش جدید باشد.

طراحی موجود با استفاده از مدل رابطه‌ای، Eloquent ORM، قیود یکتا، تراکنش‌های پایگاه داده، قفل ردیف‌ها، حذف نرم، ثبت رویداد و سرویس‌های دامنه تلاش می‌کند این مسائل را به‌صورت نظام‌مند حل کند.

## ۴. دامنه کاربرد و گروه‌های کاربری

### ۴.۱. مدیر و مدیر سامانه

نقش‌های `manager` و `admin` به بخش‌های مدیریتی، کاربران، گزارش‌ها، خدمات، فعالیت‌ها و تنظیمات دسترسی دارند. سطح دقیق دسترسی علاوه بر نقش، به آرایه مجوزهای کاربر وابسته است. وجود مجوز `full_access` دسترسی گسترده مدیریتی ایجاد می‌کند.

### ۴.۲. کاربر عادی

نقش `regular_user` برای عملیات عمومی‌تر، به‌ویژه ثبت و ویرایش مددجو، طراحی شده است. مجوزهای واقعی هر کاربر از ماتریس مجوز تعریف‌شده در مدل `User` و مقدار ذخیره‌شده برای حساب وی استخراج می‌شود.

### ۴.۳. مددکار اجتماعی

نقش `social_worker` پنل اختصاصی دارد. مددکار می‌تواند خانوارهای تحت پوشش خود را مشاهده کند، خدمت را در چارچوب سهمیه تخصیص‌یافته تحویل دهد و سابقه تحویل را پیگیری کند. اتصال حساب کاربری به رکورد `SocialWorker` مبنای تشخیص مددکار جاری است.

### ۴.۴. اپراتور توزیع

نقش `distribution_operator` برای تعریف یا مدیریت خدمات مجاز و کار در ایستگاه‌های توزیع در نظر گرفته شده است. دسترسی به ورودی، تحویل و خروج از طریق مجوزهای جداگانه کنترل می‌شود؛ در نتیجه یک اپراتور لزوماً به هر سه ایستگاه دسترسی ندارد.

### ۴.۵. اپراتور فعالیت

نقش `activity_operator` برای مشاهده فعالیت‌های تخصیص‌یافته و ثبت حضور استفاده می‌شود. داشتن نقش یا مجوز ثبت حضور به‌تنهایی کافی نیست و برای عملیات مرتبط، تخصیص اپراتور به فعالیت نیز بررسی می‌شود.

### ۴.۶. حامی کودک

نقش `child_supporter` به پروفایل حامی متصل است و پنل مستقل دارد. اطلاعات تعهد ماهانه، ترجیحات، روش یادآوری و مددجویان کودک تخصیص‌یافته در این حوزه نگهداری می‌شود.

### ۴.۷. مددجو و سرپرست

مددجو و سرپرست کاربران ورودکننده به سامانه نیستند، بلکه موجودیت‌های اصلی دامنه‌اند. برای هر یک می‌توان هویت QR صادر کرد تا شناسایی در فرایندهای حضوری و عملیاتی سریع‌تر شود.

## ۵. فناوری‌ها، زبان‌ها، چارچوب‌ها و ابزارها

### ۵.۱. پشته سمت سرور

| فناوری | نقش در پروژه |
|---|---|
| PHP 8.2+ | زبان اصلی اجرای منطق سمت سرور |
| Laravel 11 | چارچوب وب، مسیریابی، احراز هویت، مجوزدهی، ORM، migration، صف، cache و پیکربندی |
| Eloquent ORM | نگاشت مدل‌های دامنه به جداول و تعریف روابط |
| Livewire 3 | ساخت صفحات تعاملی سرورمحور بدون نیاز به SPA مستقل |
| Blade | قالب‌سازی HTML و تولید رابط سمت سرور |
| Laravel HTTP Client | فراخوانی سرویس OpenAI در قابلیت‌های اختیاری هوش مصنوعی |
| PHPUnit 11 | آزمون‌های واحد و ویژگی |

نسخه‌های مهم مشاهده‌شده در قفل وابستگی‌های Composer عبارت‌اند از:

| بسته | نسخه قفل‌شده | کاربرد |
|---|---:|---|
| `laravel/framework` | 11.53.1 | هسته چارچوب |
| `livewire/livewire` | 3.7.3 | رابط تعاملی سرورمحور |
| `barryvdh/laravel-dompdf` | 3.1.2 | تولید PDF |
| `maatwebsite/excel` | 3.1.69 | خروجی Excel |
| `simplesoftwareio/simple-qrcode` | 4.2.0 | تولید QR Code |
| `doctrine/dbal` | 4.4.3 | تحلیل و تغییر schema در برخی migrationها |
| `phpunit/phpunit` | 11.5.55 | اجرای آزمون |
| `laravel/pint` | 1.26.0 | قالب‌بندی کد PHP |
| `laravel/sail` | 1.51.0 | محیط توسعه Docker اختیاری |

### ۵.۲. پشته رابط کاربری

| فناوری | نقش در پروژه |
|---|---|
| Vite 6 | ساخت و بسته‌بندی assetهای فرانت‌اند |
| Tailwind CSS 3 | کلاس‌های utility و استایل برخی مؤلفه‌ها |
| Bootstrap 5 RTL | ساختار و مؤلفه‌های رابط راست‌به‌چپ |
| Bootstrap Icons | مجموعه آیکون |
| Sass | پردازش استایل و سفارشی‌سازی Bootstrap |
| Alpine.js | مدیریت وضعیت محلی و تعاملات سبک مرورگر |
| Axios | زیرساخت درخواست HTTP در فرانت‌اند |
| Jalali Datepicker | انتخاب تاریخ جلالی |
| `html5-qrcode` | دسترسی به دوربین و اسکن QR |
| `zxing-wasm` | موتور جایگزین یا تکمیلی تشخیص بارکد و QR |
| Popper | جانمایی tooltip، popover و مؤلفه‌های Bootstrap |

رابط کاربری از فونت محلی IRANSansX و جهت راست‌به‌چپ استفاده می‌کند. ورودی‌های تاریخ و مبلغ برای زمینه فارسی بومی‌سازی شده‌اند.

### ۵.۳. وابستگی‌های نیازمند تأیید

- بسته JavaScript با نام `audit` در `package.json` ثبت شده، اما کاربرد مستقیم و روشن آن در کدهای فرانت‌اند مشاهده نشد.
- بسته JavaScript رسمی `openai` نصب شده است، ولی پیاده‌سازی واقعی قابلیت هوش مصنوعی در کد PHP و از طریق Laravel HTTP Client انجام می‌شود. نیاز به بسته فرانت‌اند OpenAI باید بازبینی شود.
- اسکریپت `npm start` فرمان `node index.js` را اجرا می‌کند، اما فایل `index.js` در فهرست پروژه مشاهده نشد. این اسکریپت در وضعیت فعلی احتمالاً قابل استفاده نیست و باید حذف یا اصلاح شود.

## ۶. ماهیت و معماری کلی سیستم

### ۶.۱. سبک معماری

پروژه یک معماری لایه‌ای عمل‌گرا بر بستر Laravel دارد. مرزبندی کامل معماری پاک یا دامنه‌محور در تمام بخش‌ها اعمال نشده، اما چند لایه مشخص قابل تشخیص است:

```text
مرورگر کاربر
    │
    ├── Blade / Livewire Views
    ├── Alpine.js و ابزارهای اسکن و تاریخ
    │
    ▼
Routes و Middleware
    │
    ├── Livewire Components
    └── HTTP Controllers
            │
            ├── Application/Domain Services
            ├── Query Objects و Report Registries
            ├── Exports
            └── Eloquent Models
                    │
                    ▼
               پایگاه داده

سرویس‌های جانبی:
OpenAI API، تولید PDF، Excel، QR و فضای ذخیره‌سازی فایل
```

### ۶.۲. لایه ارائه

لایه ارائه شامل `routes/web.php`، کنترلرها، کلاس‌های Livewire و قالب‌های Blade است. بیشتر صفحات مستقیماً به یک کلاس Livewire نگاشت شده‌اند. عملیات تعاملی مانند جست‌وجو، صفحه‌بندی، ثبت فرم، تغییر وضعیت و بازکردن پنجره‌ها از طریق درخواست‌های Livewire انجام می‌شود.

### ۶.۳. لایه کاربرد و خدمات

منطق‌هایی که نیازمند تراکنش، هماهنگی چند مدل یا قواعد قابل استفاده مجدد هستند، در `app/Services` قرار گرفته‌اند. نمونه‌های مهم عبارت‌اند از:

- `ActivityCheckInService`
- `ActivityLifecycleService`
- `ActivityServiceDeliverySyncService`
- `QrIdentityService`
- `HouseholdTransferService`
- `SyncPersonFamilyContext`
- `BeneficiaryCaseFileTimeline`
- سرویس‌های هوش مصنوعی

### ۶.۴. لایه پرس‌وجو و گزارش

کلاس‌های `app/Queries` منطق جست‌وجو و گزارش پیچیده را از مؤلفه‌های UI جدا می‌کنند. زیرسامانه گزارش مددجو علاوه بر Query Object از registry و criteria نسخه‌دار استفاده می‌کند تا فیلدها، ستون‌ها، معناشناسی و ساختار فیلترها قابل کنترل باشند.

### ۶.۵. لایه داده

مدل‌های Eloquent در `app/Models` ساختار دامنه، روابط، castها، scopeها و برخی محاسبات را دربرمی‌گیرند. migrationها schema و قیود را مدیریت می‌کنند. حذف نرم در موجودیت‌های حساس مانند مددجو، سرپرست، کاربر، خدمت و تحویل خدمت به کار رفته است.

### ۶.۶. معماری داشبورد

مسیرهای متعدد مدیریتی به `App\Livewire\Admin\DashboardHome` وارد می‌شوند. این مؤلفه بر اساس مسیر یا پارامتر بخش، مؤلفه تخصصی مربوط را در یک پوسته داشبورد مشترک بارگذاری می‌کند. این روش یک تجربه ناوبری یکپارچه ایجاد می‌کند، اما وابستگی DashboardHome به فهرست بخش‌ها را افزایش می‌دهد.

پنل‌های مددکار اجتماعی، اپراتور توزیع، اپراتور فعالیت و حامی کودک layoutهای مستقل دارند و از نوار کناری و ناوبری متناسب با نقش خود استفاده می‌کنند.

## ۷. ساختار پوشه‌ها و فایل‌های مهم

نمای زیر بر فایل‌ها و پوشه‌های دارای نقش معماری یا دامنه‌ای تمرکز دارد و dependencyها، cache، build و فایل‌های کم‌اهمیت را خلاصه می‌کند:

```text
avayeh_hamdeli_center/
├── app/
│   ├── Console/Commands/              # فرمان‌های نگهداری و backfill
│   ├── Contracts/Ai/                  # قراردادهای قابلیت‌های هوش مصنوعی
│   ├── Exceptions/                    # خطاهای اختصاصی AI و دامنه
│   ├── Exports/                       # خروجی‌های Excel گزارش‌ها
│   ├── Helpers/                       # اعداد فارسی، تاریخ جلالی و کپی کتابخانه‌ها
│   ├── Http/Controllers/              # QR، حضور فعالیت و پیوست پرونده
│   ├── Livewire/
│   │   ├── Activities/                # تعریف، فهرست و اسکن فعالیت
│   │   ├── ActivityOperators/         # پنل اپراتور فعالیت
│   │   ├── Admin/                     # داشبورد، کاربران و گزارش‌های مدیریتی
│   │   ├── Auth/                      # ورود
│   │   ├── ChildSupporters/           # ثبت و مدیریت حامی
│   │   ├── DistributionOperators/     # تعریف خدمت، سهمیه و دروازه‌های توزیع
│   │   ├── Guardians/                 # سرپرستان و گزارش‌ها
│   │   ├── People/                    # مددجویان، پرونده و گزارش پیشرفته
│   │   ├── Services/                  # کاتالوگ، مدیریت، تحویل و گزارش خدمت
│   │   ├── Shared/                    # مؤلفه‌های مشترک مانند QR
│   │   └── SocialWorkers/             # مددکاران، انتقال خانوار و تحویل خدمت
│   ├── Models/                        # مدل‌های Eloquent
│   ├── Observers/                     # ثبت تغییرات و واکنش به رویداد مدل
│   ├── Providers/AppServiceProvider.php
│   ├── Queries/                       # پرس‌وجوهای پیچیده و قابل استفاده مجدد
│   ├── Reports/People/                # تعریف معیارها و ستون‌های گزارش مددجو
│   ├── Services/                      # سرویس‌های کاربرد و منطق تجاری
│   ├── Support/Images/                # ذخیره بهینه تصویر
│   ├── Traits/                        # رفتارهای مشترک
│   └── View/Components/               # مؤلفه‌های Blade
├── bootstrap/app.php                  # bootstrap، middleware و مدیریت خطا
├── config/                            # تنظیمات Laravel و سرویس‌های خارجی
├── database/
│   ├── factories/
│   ├── migrations/                    # ۱۱۹ فایل تکامل schema
│   └── seeders/                       # داده‌های مرجع و حساب مدیر اولیه
├── public/                            # نقطه ورود وب، assetهای build و فایل‌های عمومی
├── resources/
│   ├── css/app.css
│   ├── js/                            # entry و افزونه‌های اسکن/تاریخ
│   ├── sass/app.scss
│   └── views/                         # layoutها، صفحات و templateهای Livewire
├── routes/
│   ├── web.php                        # مسیرهای وب
│   └── console.php                    # فرمان کنسولی نمونه
├── storage/                           # log، cache و فایل‌های runtime
├── tests/
│   ├── Feature/
│   └── Unit/
├── composer.json
├── package.json
├── phpunit.xml
└── vite.config.js
```

## ۸. تحلیل اجزای اصلی و فایل‌های کلیدی

### ۸.۱. `routes/web.php`

این فایل نقطه تعریف تمام مسیرهای HTTP کسب‌وکاری است. مسیرها عمدتاً با middleware احراز هویت و Gateهای `can:*` محافظت می‌شوند. ترتیب مسیرها اهمیت دارد؛ برای نمونه مسیرهای ثابت گزارش و بلاک‌لیست پیش از مسیر پارامتری `/people/{mode}/{person?}` تعریف شده‌اند تا با آن تداخل نکنند.

در پروژه `routes/api.php` یا API عمومی نسخه‌بندی‌شده مشاهده نشد. تنها endpoint دارای خروجی صریح JSON، ثبت حضور QR فعالیت است. بنابراین سامانه در وضعیت فعلی برای مصرف مستقیم کاربران وب و Livewire طراحی شده، نه برای ارائه API عمومی به سامانه‌های ثالث.

### ۸.۲. `bootstrap/app.php`

این فایل در Laravel 11 وظیفه راه‌اندازی برنامه را بر عهده دارد و موارد زیر را تنظیم می‌کند:

- بارگذاری مسیرهای وب و کنسول؛
- health endpoint در `/up`؛
- اعتماد به reverse proxy بر اساس `TRUSTED_PROXIES`؛
- مدیریت ویژه خطاهای authorization؛
- هدایت کاربر احراز‌شده به نخستین پنل مجاز در صورت مراجعه به بخش غیرمجاز؛
- ثبت هشدار در log برای redirect ایمن ناشی از عدم مجوز.

این رفتار باعث می‌شود کاربر دارای یک پنل معتبر به‌جای مواجهه با صفحه خطای خام، به بخش مجاز خود هدایت شود. برای درخواست‌هایی که JSON انتظار دارند این redirect اعمال نمی‌شود.

### ۸.۳. `app/Providers/AppServiceProvider.php`

این Provider بخشی مهم از سیم‌کشی سامانه است:

- Observerهای مددجو و سرپرست را ثبت می‌کند؛
- morph map صریح برای موجودیت‌های QR تعریف می‌کند؛
- قراردادهای AI را به پیاده‌سازی‌های OpenAI متصل می‌کند؛
- Gateهای دسترسی به مدیریت مددجویان، مدیر، مددکار، اپراتور توزیع، حامی و اپراتور فعالیت را ثبت می‌کند؛
- دسترسی به خدمت مشخص را با نقش، مالکیت یا مجوز ترکیب می‌کند.

استفاده از morph map باعث می‌شود مقدار نوع موجودیت در پایگاه داده به نام کلاس PHP وابستگی مستقیم نداشته باشد و تغییر namespace خطر کمتری برای داده‌های QR ایجاد کند.

### ۸.۴. `app/Models/User.php`

مدل `User` فقط مدل احراز هویت نیست و بخش مهمی از سیاست دسترسی را در خود دارد. این کلاس:

- هفت سطح دسترسی سامانه را تعریف می‌کند؛
- فهرست مجوزها و مجوزهای مجاز برای هر نقش را نگهداری می‌کند؛
- مجوزهای ثبت، ویرایش و حذف مددجو را تفکیک می‌کند؛
- دسترسی سه ایستگاه توزیع و مدیریت خدمت را جدا می‌سازد؛
- دسترسی کامل و ثبت حضور فعالیت را مدل می‌کند؛
- مجوزها را normalize می‌کند تا مجوز نامعتبر یا ناسازگار با نقش ذخیره نشود؛
- رابطه کاربر با مددکار، پروفایل حامی و فعالیت‌های تخصیص‌یافته را برقرار می‌کند؛
- روش‌های تشخیص نوع پنل و توانایی‌های کاربر را در اختیار Gateها قرار می‌دهد.

مدل بزرگ است و علاوه بر داده، سیاست دسترسی، همگام‌سازی نقش و رفتار پروفایل‌ها را نیز انجام می‌دهد. این تمرکز، یافتن قواعد را آسان کرده، ولی مسئولیت کلاس را افزایش داده است.

### ۸.۵. `app/Livewire/People/CreatePerson.php`

این مؤلفه با حدود ۲٬۵۶۴ خط و ۸۲ متد، فرم جامع ثبت و ویرایش مددجو را مدیریت می‌کند. فرم در ده مرحله طراحی شده است:

1. اطلاعات فردی؛
2. مهارت‌ها؛
3. معلولیت؛
4. مدارک؛
5. تحصیل؛
6. وضعیت خانواده؛
7. سرپرست؛
8. اطلاعات بانکی؛
9. محل سکونت و تماس؛
10. پوشش حمایتی و سطح نیاز.

کلاس مسئول بارگذاری داده‌های مرجع، اعتبارسنجی مرحله‌ای، مدیریت فایل، تبدیل تاریخ، یافتن یا ساخت سرپرست، ذخیره روابط و کنترل حالت ثبت/ویرایش است. عملیات نهایی در تراکنش انجام می‌شود تا نیمه‌کاره ماندن پرونده باعث ناسازگاری میان جداول وابسته نشود.

وجود فایل `CreatePerson.php.bak` در کنار کد اصلی یک بدهی نگهداری است؛ فایل پشتیبان باید از مخزن حذف شود و تاریخچه تغییرات به Git واگذار گردد.

### ۸.۶. `app/Livewire/People/FastCreatePerson.php`

این مؤلفه برای ثبت سریع اطلاعات حداقلی مددجو طراحی شده است. مسیر آن با مجوزهای ثبت یا ویرایش کنترل می‌شود و در سناریوهایی که ورود کامل ده‌مرحله‌ای مناسب نیست، امکان ایجاد پرونده اولیه را فراهم می‌کند. تکمیل بعدی پرونده توسط صف پرونده‌های ناقص و شاخص‌های completeness قابل پیگیری است.

### ۸.۷. `app/Livewire/People/AdvancedFilterBuilder.php`

این مؤلفه رابط ساخت گزارش پیشرفته مددجویان است. مسئولیت آن ترکیب معیارهای چندگانه، انتخاب ستون‌ها، ذخیره فیلتر، تولید خروجی و در صورت فعال بودن، تبدیل درخواست زبان طبیعی به معیار ساخت‌یافته است. منطق اصلی query در کلاس‌های `Queries/People` و تعریف فیلدها در `Reports/People` جدا شده است.

### ۸.۸. `app/Livewire/DistributionOperators/ServiceBatchCreator.php`

این مؤلفه با حدود ۳٬۳۱۰ خط و ۱۴۱ متد بزرگ‌ترین کلاس مشاهده‌شده در منطق رابط است. تعریف خدمات از پیش‌تعریف‌شده و متفرقه، دسته‌ها، واحدها، بازه زمانی، کانال تحویل، مددکاران و سهمیه‌ها را هماهنگ می‌کند.

کلاس از نظر دامنه مهم است، اما حجم و تعداد مسئولیت‌های آن نشان می‌دهد که باید به Form Object، Validator، Service و مؤلفه‌های کوچک‌تر شکسته شود. در وضعیت فعلی تغییر قواعد تعریف خدمت می‌تواند دامنه بزرگی از UI و منطق ذخیره‌سازی را تحت تأثیر قرار دهد.

### ۸.۹. دروازه‌های توزیع

سه مؤلفه زیر زنجیره توزیع ایستگاهی را پیاده می‌کنند:

- `DistributionOperators/Gates/EntryGate.php`
- `DistributionOperators/Gates/DeliveryGate.php`
- `DistributionOperators/Gates/ExitGate.php`

کلاس پایه `DistributionGatePage` رفتار مشترک انتخاب خدمت، دریافت‌کننده، اسکن و نمایش داده را دربرمی‌گیرد. هر ایستگاه یک مجوز مستقل دارد و وضعیت `GateEntryAssignment` را در مرحله متناسب تغییر می‌دهد.

### ۸.۱۰. `app/Livewire/SocialWorkers/Dashboard.php`

داشبورد مددکار با حدود ۱٬۳۷۰ خط و ۵۹ متد، خانوارهای تحت پوشش، خدمات قابل تحویل، دسته‌های خدمت، سهمیه‌ها و ثبت تحویل را مدیریت می‌کند. متد ذخیره تحویل از تراکنش و `lockForUpdate` استفاده می‌کند تا سهمیه و موجودی هم‌زمان توسط چند درخواست مصرف نشود.

### ۸.۱۱. `app/Services/QrIdentityService.php`

این سرویس چرخه عمر هویت QR را مدیریت می‌کند:

- صدور QR برای مددجو یا سرپرست؛
- ساخت کد عمومی خوانا؛
- تولید توکن تصادفی؛
- محاسبه هش HMAC-SHA256 با کلید برنامه؛
- resolve کردن توکن؛
- ابطال یا جایگزینی QR با ثبت دلیل؛
- ثبت نتیجه اسکن، کاربر، IP و User-Agent.

ذخیره توکن با cast رمزنگاری‌شده و نگهداری هش برای تطبیق، دفاع چندلایه‌ای در برابر افشای مستقیم داده ایجاد می‌کند.

### ۸.۱۲. سرویس‌های فعالیت

`ActivityLifecycleService` انتقال وضعیت فعالیت را کنترل می‌کند. `ActivityCheckInService` صحت فعالیت، مجوز اپراتور، تخصیص، ظرفیت و حضور تکراری را بررسی می‌کند. `ActivityServiceDeliverySyncService` میان حضور فعالیت و خدمات متصل هماهنگی ایجاد می‌کند.

تفکیک این سه مسئولیت از مؤلفه Livewire یک نقطه قوت معماری است؛ قواعد اصلی فعالیت به رابط خاص وابسته نیستند و قابلیت آزمون مستقل دارند.

### ۸.۱۳. `BeneficiaryCaseFileTimeline`

این سرویس چند منبع داده را در یک خط زمانی واحد ادغام می‌کند:

- تحویل مستقیم خدمت به مددجو؛
- تحویل خانوادگی از طریق سرپرست؛
- حضور در فعالیت؛
- یادداشت‌ها، فاکتورها، پیگیری‌ها و سوابق دستی؛
- پیوست‌های پرونده.

خروجی یک timeline مرتب‌شده ایجاد می‌کند تا کارشناس به‌جای بررسی چند جدول، تاریخچه متمرکز مددجو را مشاهده کند.

### ۸.۱۴. Query Objectها

پوشه `app/Queries` شامل پرس‌وجوهای فهرست، جزئیات و گزارش برای مددجویان و سرپرستان است. این کلاس‌ها مزایای زیر را دارند:

- کاهش SQL و شرط‌های پراکنده در مؤلفه‌های Livewire؛
- امکان استفاده مجدد در صفحه و export؛
- بهبود آزمون‌پذیری؛
- امکان سازگاری شرطی با MySQL و SQLite؛
- کنترل eager loading و جلوگیری از N+1 در گزارش‌های مهم.

### ۸.۱۵. Exportها

کلاس‌های `app/Exports` خروجی Excel برای مددجویان، سرپرستان، مددکاران، خدمات، پرونده، حضور فعالیت و گزارش سه دروازه تولید می‌کنند. تولید PDF در بخش گزارش مددجو از DOMPDF و قالب Blade اختصاصی استفاده می‌کند.

## ۹. ماژول‌های اصلی سامانه

### ۹.۱. مدیریت مددجویان

قابلیت‌های اصلی این ماژول عبارت‌اند از:

- ثبت سریع و ثبت کامل؛
- ویرایش پرونده؛
- جست‌وجوی نرمال‌شده نام و کد ملی؛
- مشاهده جزئیات؛
- حذف نرم با دلیل؛
- مشاهده و بازیابی از بلاک‌لیست؛
- گزارش پیشرفته؛
- تشخیص پرونده ناقص؛
- ثبت تاریخچه تغییرات؛
- صدور QR؛
- دسترسی به پرونده زمانی.

Observer مددجو تغییرات مهم را در `beneficiary_audit_logs` ثبت می‌کند. ستون‌های normalized search و فرمان backfill برای افزایش دقت و کارایی جست‌وجوی فارسی افزوده شده‌اند.

### ۹.۲. مدیریت سرپرستان و خانوار

سرپرست نقطه اتصال اعضای خانوار است. اطلاعات شغلی، بیمه، وسیله نقلیه، تعداد اعضای اضافی، مددکار مسئول، نشانی و تماس می‌تواند در سطح سرپرست نگهداری شود. قابلیت‌های این حوزه شامل ویرایش، گزارش، فیلتر ذخیره‌شده، حذف نرم خانوار و مشاهده اعضاست.

سرویس `SyncPersonFamilyContext` برخی وضعیت‌های خانوادگی و آسیب‌های مرتبط را میان خواهران و برادرانی که والدین مشترک دارند منتشر می‌کند. این همگام‌سازی برای جلوگیری از تناقض پرونده‌های یک خانواده انجام می‌شود، اما دامنه دقیق فیلدهای قابل انتشار باید همراه با سیاست کارشناسی مرکز نگهداری و مستند شود.

### ۹.۳. مدیریت مددکاران اجتماعی

این ماژول شامل ثبت، ویرایش، غیرفعال‌سازی، حذف نرم، گزارش، جست‌وجو، مشاهده خانوارهای تحت پوشش و انتقال خانوار است. `HouseholdTransferService` انتقال خانوار از مددکار مبدأ به مقصد را به‌صورت متمرکز انجام می‌دهد.

مددکار به حساب کاربری متصل می‌شود و `Guardian`ها به مددکار تخصیص می‌یابند. دسترسی مستقیم به مددجویان تحت پوشش از طریق رابطه خانوار قابل محاسبه است.

### ۹.۴. کاتالوگ و برنامه خدمات

ساختار خدمات چندسطحی است:

- `ServiceName`: نام یا نوع پایه خدمت؛
- `ServiceCategoryTemplate`: الگوی دسته‌های قابل استفاده برای یک نام خدمت؛
- `Service`: رخداد اجرایی، برنامه یا کمپین خدمت؛
- `ServiceCategory`: قلم واقعی خدمت با مقدار، واحد، ارزش و تصویر اختیاری؛
- `ServiceWorkerAllocation`: سهمیه یک مددکار برای یک دسته از خدمت؛
- `ServiceDelivery`: ثبت نهایی تحویل.

این تفکیک باعث می‌شود «تعریف مفهومی خدمت» از «اجرای یک نوبت مشخص توزیع» جدا باشد.

### ۹.۵. توزیع در ایستگاه

فرایند توزیع ایستگاهی سه مرحله دارد:

1. **ورودی:** شناسایی مددجو یا سرپرست و تخصیص اقلام مجاز؛
2. **تحویل:** تأیید فیزیکی تحویل هر قلم؛
3. **خروج:** نهایی‌سازی و انتقال اقلام تأییدشده به دفتر `service_deliveries`.

این ساختار تفکیک وظایف و قابلیت حسابرسی را بهبود می‌دهد و از نهایی‌شدن قلمی که واقعاً تحویل نشده جلوگیری می‌کند.

### ۹.۶. فعالیت‌ها

فعالیت می‌تواند آموزشی، فرهنگی، حمایتی یا از نوع تعریف‌شده در گزینه‌های مدل باشد. هر فعالیت دارای کد خودکار `ACT-xxxxx`، زمان شروع و پایان، ظرفیت، وضعیت، توضیح، اپراتورهای تخصیص‌یافته و فهرست حضور است.

اگر خدمت‌هایی به فعالیت متصل باشند، ثبت حضور می‌تواند به‌طور خودکار تحویل‌های کانال `activity` را ایجاد کند. این ارتباط، حضور و بهره‌مندی از خدمت را در یک عملیات منسجم ثبت می‌کند.

### ۹.۷. حامیان کودک

ثبت حامی در یک wizard پنج‌مرحله‌ای انجام می‌شود. اطلاعاتی مانند مشخصات حساب کاربری، تعهد ماهانه، ترجیحات انتخاب کودک، روش یادآوری و وضعیت ارتباط شبکه اجتماعی ثبت می‌گردد. حامی می‌تواند با چند مددجو و هر مددجو با چند حامی رابطه داشته باشد.

### ۹.۸. پرونده مددجو

پرونده مددجو علاوه بر اطلاعات پایه، یک فضای ثبت سوابق کارشناسی دارد. انواع رکوردهای دستی شامل یادداشت، خدمت، فاکتور و پیگیری است. هر رکورد می‌تواند پیوست داشته باشد و زمان ثبت، ثبت‌کننده و جزئیات آن نگهداری می‌شود.

### ۹.۹. گزارش‌ها و فیلترهای ذخیره‌شده

گزارش‌های مددجو، سرپرست، مددکار، خدمت، فعالیت و دروازه‌ها وجود دارند. فیلترهای ذخیره‌شده برای مددجو، سرپرست و مددکار نگهداری می‌شوند. در گزارش مددجو، `criteria_version` برای مدیریت تکامل ساختار معیارها به schema افزوده شده است.

### ۹.۱۰. یادآوری داشبورد

مدل `DashboardReminder` امکان نگهداری یادآوری‌های مدیریتی را فراهم می‌کند. بخشی از پیشنهادهای AI پرونده نیز می‌تواند برای ایجاد یا پیشنهاد یادآوری استفاده شود؛ ایجاد نهایی و دامنه نمایش آن باید مطابق رفتار UI بررسی شود.

## ۱۰. مدل داده و پایگاه داده

### ۱۰.۱. نوع پایگاه داده

پیکربندی نمونه توسعه از SQLite استفاده می‌کند، اما پیکربندی تولید بررسی‌شده MySQL است. `phpunit.xml` نیز اجرای آزمون‌ها را به یک پایگاه MySQL محلی با نام `avayeh_hamdeli_center` اجبار می‌کند. در بخش‌هایی از queryها و migrationها سازگاری چند پایگاه داده در نظر گرفته شده، بااین‌حال بعضی migrationهای تکاملی از منطق مخصوص MySQL یا `information_schema` استفاده می‌کنند.

بر اساس migrationهای موجود، حدود ۶۴ جدول ایجاد می‌شود که بخشی از آن‌ها جداول زیرساخت Laravel هستند.

### ۱۰.۲. گروه‌بندی جداول

| گروه | جداول مهم | نقش |
|---|---|---|
| هویت و دسترسی | `users`, `sessions`, `password_reset_tokens`, `user_action_requests` | حساب کاربری، نشست، بازیابی و درخواست اقدام |
| مددجو | `people`, `beneficiary_audit_logs`, `beneficiary_case_records`, `beneficiary_case_record_attachments` | پرونده پایه، حسابرسی و سوابق زمانی |
| خانوار | `guardians`, `family_statuses`, `residences`, `contacts`, `bank_infos` | سرپرست و وضعیت مشترک خانوار |
| آموزش و حمایت | `educations`, `support_coverages`, `needs_levels` | تحصیل، سازمان حمایتی و سطح نیاز |
| داده مرجع | `districts`, `occupations`, `job_types`, `insurance_types`, `banks`, `skills`, `harm_types` و موارد مشابه | گزینه‌های استاندارد فرم‌ها |
| مددکار | `social_workers`, `guardian_saved_filters`, `social_worker_saved_filters` | اطلاعات مددکار و گزارش‌های ذخیره‌شده |
| خدمت | `service_names`, `service_category_templates`, `services`, `service_categories`, `service_units`, `service_social_worker`, `service_deliveries` | کاتالوگ، برنامه، سهمیه و دفتر تحویل |
| توزیع | `gate_entry_assignments`, `service_entry_fields`, `gate_entry_field_values` | وضعیت توزیع و فیلدهای سفارشی ورودی |
| فعالیت | `activities`, `activity_attendances`, `activity_operator_assignments` | فعالیت، حضور و اپراتور |
| حامی | `sponsor_profiles`, `person_sponsor_profile` | پروفایل حامی و تخصیص مددجو |
| QR | `qr_identities`, `qr_identity_scan_logs` | هویت QR و تاریخچه اسکن |
| زیرساخت | `cache`, `cache_locks`, `jobs`, `job_batches`, `failed_jobs` | cache و صف Laravel |

### ۱۰.۳. روابط هسته مددجو و خانوار

```text
SocialWorker 1 ─── N Guardian 1 ─── N Person
                            │             │
                            │             ├── 1 Education
                            │             ├── 1 FamilyStatus
                            │             ├── 1 SupportCoverage
                            │             ├── 1 NeedsLevel
                            │             ├── N Skills
                            │             ├── N HarmTypes
                            │             └── N CaseRecords
                            │
                            ├── Residence / Contact / BankInfo
                            └── N Family ServiceDeliveries
```

برخی اطلاعات اقامت و تماس می‌توانند به شخص یا سرپرست وابسته باشند. قیود یکتا برای جلوگیری از چند رکورد فعال ناسازگار در جداول وابسته طی migrationهای تکاملی افزوده شده‌اند.

### ۱۰.۴. روابط خدمت

```text
ServiceName 1 ─── N Service
      │              │
      ├── N ServiceCategoryTemplate
      │              │
      └── N ServiceCategory ◄── N ServiceWorkerAllocation
                              │
Service 1 ────────────────────┤
                              └── N ServiceDelivery

SocialWorker 1 ─── N ServiceWorkerAllocation
Person/Guardian ─── N ServiceDelivery
Activity 1 ──────── N ServiceDelivery (برای کانال activity)
```

جدول `service_social_worker` در ابتدا pivot ساده بوده و سپس به ساختار سهمیه دسته‌محور توسعه یافته است. مدل `ServiceWorkerAllocation` این جدول را به‌عنوان موجودیت دارای داده‌های مستقل مانند مقدار تخصیص و تخصیص‌دهنده به کار می‌گیرد.

### ۱۰.۵. روابط توزیع

هر `GateEntryAssignment` یک دریافت‌کننده، خدمت و دسته خدمت را به وضعیت توزیع متصل می‌کند. در زمان خروج، `ServiceDelivery` با کلید `gate_entry_assignment_id` ساخته می‌شود. یکتایی این کلید مانع تبدیل دوباره یک تخصیص به تحویل نهایی می‌شود.

### ۱۰.۶. روابط فعالیت

`Activity` با `Person` از طریق `activity_attendances` رابطه چندبه‌چند دارد. رکورد حضور اطلاعات زمان، ثبت‌کننده و روش ثبت را نگهداری می‌کند. رابطه اپراتورها نیز از طریق `activity_operator_assignments` برقرار می‌شود.

### ۱۰.۷. روابط حامی

`SponsorProfile` به یک `User` متصل است و با `Person` از طریق `person_sponsor_profile` رابطه چندبه‌چند دارد. اطلاعات تخصیص می‌تواند شامل زمان و ثبت‌کننده باشد.

### ۱۰.۸. QR به‌صورت polymorphic

جدول `qr_identities` با `subject_type` و `subject_id` به `Person` یا `Guardian` متصل می‌شود. morph map مقادیر پایدار `person` و `guardian` را به کلاس‌ها نگاشت می‌کند.

### ۱۰.۹. حذف نرم و تمامیت داده

حذف نرم برای نگهداری تاریخچه و امکان بازیابی در چند موجودیت اصلی استفاده شده است. در عملیات تخصیص مجدد، برخی رکوردهای حذف‌شده به‌جای ایجاد نسخه تکراری بازیابی می‌شوند. migrationها قیود یکتا، indexهای گزارش و indexهای گیرنده/تاریخ را به‌تدریج افزوده‌اند.

## ۱۱. مسیرها و endpointهای مهم

### ۱۱.۱. احراز هویت و عمومی

| متد | مسیر | نام | کاربرد | حفاظت |
|---|---|---|---|---|
| GET | `/login` | `login` | نمایش فرم ورود | `guest` |
| POST | `/logout` | `logout` | پایان نشست | احراز هویت جاری |
| GET | `/` | — | هدایت بر اساس وضعیت ورود و پنل مجاز | — |
| GET | `/up` | — | health check چارچوب | عمومی |
| GET | `/qr/r/{token}` | `qr-identities.resolve` | resolve هویت QR | `auth` |

### ۱۱.۲. مددجو، سرپرست و مددکار

| متد | مسیر | کاربرد | مجوز |
|---|---|---|---|
| GET | `/people` | فهرست مددجویان | `manage-people` |
| GET | `/people/fast-create/{person?}` | ثبت یا ویرایش سریع | کنترل در مؤلفه و احراز هویت |
| GET | `/people/advanced-reporting` | گزارش پیشرفته | `full-access` |
| GET | `/people/block-list` | فهرست حذف‌شده‌ها | `people-delete` |
| GET | `/people/{mode}/{person?}` | فرم جامع ثبت/ویرایش | احراز هویت و کنترل مجوز در مؤلفه |
| GET | `/guardians` | فهرست سرپرستان | `full-access` |
| GET | `/guardians/{guardian}/edit` | ویرایش سرپرست | `full-access` |
| GET | `/social-workers` | فهرست مددکاران | `manage-social-workers` |
| GET | `/social-workers/create` | ثبت مددکار | `manage-social-workers` |
| GET | `/social-workers/{socialWorker}/edit` | ویرایش مددکار | `manage-social-workers` |

### ۱۱.۳. مدیریت

مسیر `/admin/dashboard` و مسیرهای بخش‌های پرونده، خدمات، گزارش‌ها، فعالیت‌ها و کاربران به `DashboardHome` نگاشت می‌شوند. مسیرهای تغییر ساختاری معمولاً به `full-access` و مسیرهای مشاهده‌ای گسترده‌تر به `access-admin-panel` نیاز دارند.

پیوست پرونده از مسیر زیر ارائه می‌شود:

| متد | مسیر | کاربرد |
|---|---|---|
| GET | `/admin/people/case-file/attachments/{attachment}` | بررسی مجوز و stream امن پیوست |

این روش از انتشار مستقیم URL فایل حساس در فضای عمومی جلوگیری می‌کند.

### ۱۱.۴. پنل مددکار

| مسیر | کاربرد |
|---|---|
| `/social-worker/dashboard` | خانوارها و ثبت تحویل |
| `/social-worker/delivery-history` | مشاهده و ویرایش مجاز سابقه تحویل |
| `/social-worker/system-settings/user-account` | حساب کاربری |

همه این مسیرها به Gate پنل مددکار نیاز دارند.

### ۱۱.۵. پنل اپراتور توزیع

| مسیر | کاربرد | Gate اصلی |
|---|---|---|
| `/distribution-operator/define-service` | تعریف خدمت | پنل اپراتور |
| `/distribution-operator/services` | فهرست خدمات | پنل اپراتور |
| `/distribution-operator/services/{serviceId}/edit` | ویرایش خدمت | مدیریت خدمات اپراتور |
| `/distribution-operator/services/{serviceId}/allocations` | ویرایش سهمیه | پنل اپراتور و کنترل مالکیت |
| `/distribution-operator/gates/entry` | ایستگاه ورودی | inbound |
| `/distribution-operator/gates/delivery` | ایستگاه تحویل | delivery |
| `/distribution-operator/gates/exit` | ایستگاه خروج | outbound |

### ۱۱.۶. فعالیت‌ها

| متد | مسیر | ورودی/خروجی |
|---|---|---|
| POST | `/admin/activities/{activity}/check-in/qr` | ورودی JSON شامل `token`؛ خروجی JSON نتیجه ثبت حضور |
| GET | `/activity-operator/activities` | فهرست فعالیت‌های مجاز |
| GET | `/activity-operator/activities/{id}/check-in` | صفحه ثبت حضور |
| GET | `/activity-operator/reports` | گزارش حضور |

endpoint ثبت حضور علاوه بر احراز هویت و Gate، در سرویس دامنه وضعیت فعالیت، تخصیص اپراتور، تکراری نبودن و ظرفیت را کنترل می‌کند.

### ۱۱.۷. حامی کودک

| مسیر | کاربرد |
|---|---|
| `/admin/child-supporters/sponsor-registration` | ثبت حامی |
| `/admin/child-supporters/sponsors` | فهرست و مدیریت حامیان |
| `/child-supporter/dashboard` | پنل حامی |
| `/child-supporter/system-settings/user-account` | تنظیمات حساب حامی |

## ۱۲. احراز هویت، نقش‌ها و مجوزها

### ۱۲.۱. شیوه احراز هویت

احراز هویت مبتنی بر session Laravel است. در پیکربندی نمونه، session در پایگاه داده ذخیره می‌شود. صفحه ورود با Livewire پیاده‌سازی شده و خروج با درخواست POST انجام می‌شود.

### ۱۲.۲. نقش‌ها

| مقدار فنی | شرح |
|---|---|
| `manager` | مدیر ارشد |
| `admin` | مدیر سامانه |
| `regular_user` | کاربر عادی |
| `social_worker` | مددکار اجتماعی |
| `distribution_operator` | اپراتور توزیع |
| `child_supporter` | حامی کودک |
| `activity_operator` | اپراتور فعالیت |

### ۱۲.۳. مجوزهای صریح

| کلید | کاربرد |
|---|---|
| `people_register` | ثبت سریع و کامل مددجو |
| `people_edit` | ویرایش مددجو |
| `people_delete` | حذف نرم و بلاک‌لیست |
| `distribution_inbound_gate` | دسترسی ایستگاه ورودی |
| `distribution_delivery_gate` | دسترسی ایستگاه تحویل |
| `distribution_outbound_gate` | دسترسی ایستگاه خروج |
| `distribution_service_manage` | مدیریت خدمات اپراتور |
| `full_access` | دسترسی مدیریتی کامل |
| `activity_attendance_register` | ثبت حضور فعالیت |

### ۱۲.۴. کنترل دسترسی چندلایه

کنترل دسترسی فقط در route انجام نمی‌شود. لایه‌های زیر در کنار هم کار می‌کنند:

1. middleware احراز هویت؛
2. Gate مسیر؛
3. روش‌های مدل `User`؛
4. کنترل مالکیت یا تخصیص رکورد؛
5. validation و authorization داخل مؤلفه یا سرویس؛
6. قیود پایگاه داده.

این رویکرد برای عملیات حساس مانند ویرایش خدمت اپراتور، ثبت حضور و تحویل خدمت ضروری است.

## ۱۳. ارتباط بین بخش‌های سیستم و روند اجرای برنامه

### ۱۳.۱. bootstrap درخواست

1. وب‌سرور درخواست را به `public/index.php` می‌فرستد.
2. Laravel از طریق `bootstrap/app.php` راه‌اندازی می‌شود.
3. middlewareها، session و احراز هویت اجرا می‌شوند.
4. route متناظر در `routes/web.php` انتخاب می‌شود.
5. Gate مربوط، مجوز کاربر را ارزیابی می‌کند.
6. کنترلر یا مؤلفه Livewire اجرا می‌شود.
7. مؤلفه از Service، Query یا Model موردنیاز استفاده می‌کند.
8. Eloquent پرس‌وجو یا تراکنش را روی پایگاه داده اجرا می‌کند.
9. Blade پاسخ HTML را تولید می‌کند یا endpoint نتیجه JSON/فایل بازمی‌گرداند.
10. تعاملات بعدی Livewire به‌صورت درخواست‌های کوچک به سرور ارسال و DOM به‌روزرسانی می‌شود.

### ۱۳.۲. ورود و هدایت به پنل

پس از ورود موفق، `LoginRedirector` بر اساس نقش و مجوز، نخستین پنل مجاز را انتخاب می‌کند. اگر کاربر مستقیماً وارد یک بخش غیرمجاز شود، مدیریت خطای authorization در صورت وجود پنل معتبر، وی را به مسیر امن هدایت و رخداد را ثبت می‌کند.

### ۱۳.۳. چرخه Livewire

در صفحات Livewire، state فرم در کلاس PHP نگهداری می‌شود. رویدادهای کاربر مانند کلیک، تغییر فیلتر یا ثبت فرم به متد عمومی مؤلفه متصل‌اند. پس از validation و اجرای منطق، Livewire بخش تغییرکرده DOM را به‌روزرسانی می‌کند. Alpine.js برای stateهای بسیار محلی که نیاز به round-trip ندارند به کار می‌رود.

## ۱۴. ورودی‌ها، خروجی‌ها، جریان داده و سناریوهای اصلی

ورودی‌های سامانه در سه گروه اصلی قرار می‌گیرند: داده‌های فرم و جست‌وجوی کاربران، داده‌های اسکن‌شده از QR و فایل‌های بارگذاری‌شده. خروجی‌ها نیز شامل صفحات و اعلان‌های تعاملی، رکوردهای پایگاه داده، گزارش‌های Excel و PDF، پاسخ JSON ثبت حضور، QR تولیدشده و فایل‌های محافظت‌شده پرونده هستند. در هر جریان، مؤلفه Livewire یا کنترلر ورودی را دریافت می‌کند، validation و authorization را اجرا می‌کند، عملیات دامنه را به Service یا Model می‌سپارد و سپس نتیجه مناسب برای رابط یا export را تولید می‌کند.

### ۱۴.۱. ثبت کامل مددجو

```text
ورود کاربر مجاز
  → انتخاب ثبت کامل
  → تکمیل ده مرحله
  → اعتبارسنجی هر مرحله
  → شروع تراکنش
  → ایجاد/به‌روزرسانی سرپرست
  → ایجاد/به‌روزرسانی مددجو
  → ذخیره وضعیت خانواده، تحصیل، سکونت، تماس، بانک و حمایت
  → sync مهارت‌ها و آسیب‌ها
  → ذخیره بهینه تصاویر و مدارک
  → همگام‌سازی زمینه خانوادگی
  → ثبت audit
  → commit و نمایش نتیجه
```

ورودی‌ها شامل اطلاعات هویتی، کد ملی، اطلاعات شناسنامه، تاریخ جلالی تولد، جنسیت، وضعیت والدین، سرپرست، تحصیل، معلولیت، مهارت، آسیب، شماره تماس، نشانی، بانک و پوشش حمایتی است. خروجی، مجموعه‌ای سازگار از رکوردهای مرتبط و یک پرونده قابل گزارش است.

### ۱۴.۲. ثبت سریع و تکمیل بعدی

در ثبت سریع، حداقل اطلاعات برای ایجاد هویت مددجو دریافت می‌شود. سپس `BeneficiaryCompletenessCatalog` فیلدهای ناقص را ارزیابی می‌کند و `IncompleteCasesQueue` پرونده‌های نیازمند تکمیل را نمایش می‌دهد. cache اختصاصی این صف برای کاهش هزینه محاسبه وجود دارد و با تغییر مدل‌های مرتبط invalidation می‌شود.

### ۱۴.۳. تعریف خدمت و سهمیه مددکار

```text
انتخاب نام خدمت
  → تعیین فردی یا خانوادگی بودن
  → تعیین تاریخ، اولویت و کانال‌ها
  → تعریف دسته‌ها، واحد، مقدار و ارزش
  → انتخاب مددکاران
  → تعیین سهمیه هر مددکار برای هر دسته
  → اعتبارسنجی مجموع سهمیه‌ها
  → ذخیره Service، Category و Allocation
```

نوع خدمت:

- `individual`: تحویل به شخص؛
- `family`: تحویل به سرپرست یا خانوار.

کانال‌ها:

- `gate`: ایستگاه توزیع؛
- `home`: تحویل در منزل؛
- `activity`: تحویل وابسته به حضور فعالیت.

### ۱۴.۴. تحویل توسط مددکار

1. مددکار خدمت و خانوار تحت پوشش را انتخاب می‌کند.
2. سامانه دسته‌های تخصیص‌یافته و باقیمانده سهمیه را محاسبه می‌کند.
3. ورودی مقدار تحویل validate می‌شود.
4. تراکنش آغاز می‌شود.
5. ردیف دسته و سهمیه با `lockForUpdate` قفل می‌شوند.
6. موجودی دسته و سهمیه مددکار دوباره درون تراکنش محاسبه می‌شود.
7. تحویل‌ها با batch UUID ثبت می‌شوند.
8. مجموع تحویل و وضعیت خدمت refresh می‌شود.

محاسبه مجدد درون قفل مهم است؛ زیرا مقدار نمایش‌داده‌شده در UI ممکن است میان مشاهده و ثبت توسط درخواست دیگری تغییر کرده باشد.

### ۱۴.۵. توزیع سه‌مرحله‌ای

#### مرحله ورودی

اپراتور از QR یا جست‌وجوی دستی برای شناسایی استفاده می‌کند. دسته‌های مجاز انتخاب و برای هر دریافت‌کننده/دسته رکورد `pending` ایجاد می‌شود. فیلدهای سفارشی تعریف‌شده برای خدمت نیز در `gate_entry_field_values` ذخیره می‌شوند.

#### مرحله تحویل

اپراتور دریافت فیزیکی اقلام را تأیید می‌کند و وضعیت میان `pending` و `delivered` تغییر می‌یابد. اقلام `finalized` دیگر قابل تغییر عادی نیستند.

#### مرحله خروج

خروج فقط اقلام `delivered` را در تراکنش به `ServiceDelivery` تبدیل می‌کند و سپس assignment را `finalized` می‌سازد.

سازوکارهای ایمنی این جریان:

- کلید یکتا برای هر دریافت‌کننده/دسته؛
- بازیابی رکورد soft-deleted در تخصیص مجدد؛
- گرفتن قفل ردیف؛
- مدیریت خطای duplicate در race condition؛
- یکتایی `gate_entry_assignment_id` در دفتر تحویل؛
- idempotency در برابر دوبارکلیک و تب هم‌زمان.

### ۱۴.۶. ثبت حضور فعالیت

```text
انتخاب فعالیت
  → اسکن QR یا جست‌وجوی دستی
  → resolve مددجو
  → بررسی ongoing بودن فعالیت
  → بررسی مجوز و تخصیص اپراتور
  → بررسی حضور تکراری
  → بررسی ظرفیت
  → ثبت attendance
  → ایجاد/همگام‌سازی تحویل خدمات activity
  → بازگرداندن نتیجه
```

### ۱۴.۷. پرونده زمانی مددجو

سرویس timeline تحویل‌ها، حضورها و رکوردهای دستی را از منابع مختلف می‌خواند، آن‌ها را به ساختار نمایش مشترک تبدیل و بر اساس زمان مرتب می‌کند. کاربر می‌تواند رکورد جدید و پیوست اضافه کند یا خروجی Excel بگیرد.

### ۱۴.۸. صدور و اسکن QR

1. برای مددجو یا سرپرست توکن ۴۸کاراکتری تولید می‌شود.
2. توکن رمزنگاری و هش آن ذخیره می‌شود.
3. کد عمومی با پیشوند `PQR-` یا `GQR-` ساخته می‌شود.
4. QR به route resolve اشاره می‌کند.
5. در زمان اسکن، وضعیت فعال و تطبیق توکن بررسی می‌شود.
6. نتیجه اسکن با زمینه امنیتی ثبت می‌گردد.

## ۱۵. منطق تجاری و الگوریتم‌های مهم

### ۱۵.۱. تمامیت دسته خدمت

در زمان ثبت تحویل کنترل می‌شود که:

- دسته واقعاً متعلق به همان خدمت باشد؛
- مقدار تحویل منفی یا صفر نامعتبر نباشد؛
- مجموع تحویل از مقدار دسته بیشتر نشود؛
- در تحویل مددکار، مقدار از سهمیه همان مددکار و دسته عبور نکند.

این کنترل‌ها نباید فقط به UI متکی باشند و در مدل/سرویس نیز اعمال شده‌اند.

### ۱۵.۲. به‌روزرسانی پیشرفت خدمت

مدل `Service` مجموع `delivered_quantity` را از دفتر تحویل محاسبه می‌کند. وضعیت بر اساس میزان تحویل و تکمیل سهمیه‌ها تغییر می‌کند:

- بدون تحویل: `approved`؛
- دارای تحویل ناقص: `in_distribution`؛
- تکمیل موجودی یا سهمیه‌های مرتبط: `completed`.

وضعیت اولیه می‌تواند `draft` باشد. انتقال‌ها و شرایط دقیق ممکن است بسته به نوع خدمت و مسیر ایجاد متفاوت باشند.

### ۱۵.۳. چرخه عمر فعالیت

انتقال‌های مجاز:

```text
draft      → ongoing | cancelled
ongoing    → closed  | cancelled
closed     → cancelled (فقط در شرایط محدود)
cancelled  → بدون مقصد
```

فعالیت برای ورود به `ongoing` باید زمان شروع معتبر داشته باشد. بستن فقط از وضعیت `ongoing` ممکن است. لغو فعالیت بسته‌شده‌ای که حضور ثبت‌شده دارد محدود شده است.

### ۱۵.۴. جلوگیری از حضور تکراری

علاوه بر بررسی سطح سرویس، ساختار داده باید یکتایی منطقی حضور هر مددجو در هر فعالیت را حفظ کند. سرویس قبل از ثبت، رکورد قبلی را بررسی می‌کند و نتیجه‌ای ساخت‌یافته برای موفقیت یا خطای تکراری برمی‌گرداند.

### ۱۵.۵. همگام‌سازی خدمت فعالیت

هنگام ثبت حضور، برای هر خدمت فعال متصل به فعالیت و هر دسته واجد شرایط، `ServiceDelivery` متناظر ایجاد می‌شود. سرویس sync همچنین تغییر فهرست خدمات یک فعالیت را با حضورهای قبلی هماهنگ می‌کند تا افزودن خدمت جدید، داده‌های حضور موجود را نادیده نگیرد.

### ۱۵.۶. نرمال‌سازی جست‌وجوی فارسی

ستون‌های جست‌وجوی نرمال‌شده برای نام و شناسه‌ها به جدول `people` افزوده شده‌اند. فرمان `BackfillPeopleNormalizedSearchColumns` داده‌های قدیمی را پر می‌کند. این روش اختلاف نویسه‌های فارسی/عربی، فاصله و شکل‌های مختلف ورودی را تا حد تعریف‌شده کاهش می‌دهد.

### ۱۵.۷. completeness پرونده

`BeneficiaryCompletenessCatalog` مجموعه الزامات تکمیل پرونده را تعریف می‌کند. خروجی آن برای شمارش موارد ناقص و اولویت‌بندی صف تکمیل استفاده می‌شود. جداسازی catalog از UI امکان افزودن معیارهای جدید را فراهم می‌کند.

### ۱۵.۸. حذف نرم با دلیل

حذف مددجو و سرپرست با سرویس اختصاصی و ثبت دلیل انجام می‌شود. این رفتار برای سازمان حمایتی مناسب است، زیرا حذف فیزیکی می‌تواند سابقه تحویل و گزارش را مخدوش کند. بازیابی از بلاک‌لیست نیز قابل پشتیبانی است.

## ۱۶. رابط کاربری و مدیریت state

### ۱۶.۱. layoutها

layoutهای مجزا برای این گروه‌ها وجود دارد:

- مدیریت؛
- مددکار اجتماعی؛
- اپراتور توزیع؛
- اپراتور فعالیت؛
- حامی کودک؛
- احراز هویت.

هر layout header، sidebar و assetهای متناسب را ترکیب می‌کند. partialهای جداگانه برای نوار کناری نقش‌ها وجود دارد.

### ۱۶.۲. مؤلفه‌های مشترک

مؤلفه‌های Blade مشترک شامل اعلان‌ها، modal اطلاع‌رسانی، نمایش مبلغ، badge نوع خدمت، avatar و popover توضیح هستند. `QrIdentityModal` نیز به‌عنوان Livewire component مشترک برای نمایش و مدیریت QR به کار می‌رود.

### ۱۶.۳. state

state اصلی داده و فرم در کلاس‌های Livewire نگهداری می‌شود. Alpine.js برای موارد زیر استفاده می‌شود:

- drawer و sidebar؛
- modal و state نمایشی؛
- ورودی مبلغ ریالی؛
- تاریخ و زمان جلالی؛
- بازخورد خوش‌بینانه تحویل؛
- راه‌اندازی، توقف و تنظیم اسکنر؛
- انتخاب دوربین، zoom و torch.

این ترکیب هزینه ساخت SPA مستقل را کاهش می‌دهد، ولی بزرگ‌شدن مؤلفه‌های Livewire می‌تواند زمان نگهداری و payload را افزایش دهد.

### ۱۶.۴. اسکنر QR

فایل‌های `qr-scanner-enhancer.js` و کدهای مرتبط قابلیت‌های زیر را فراهم می‌کنند:

- dynamic import کتابخانه اسکن؛
- انتخاب دوربین پشت یا دوربین دلخواه؛
- روشن‌کردن چراغ در دستگاه سازگار؛
- zoom در صورت پشتیبانی؛
- لرزش و صدای نتیجه؛
- جلوگیری از پردازش تکراری سریع؛
- fallback مبتنی بر ZXing/WASM.

دسترسی به دوربین در مرورگرهای مدرن معمولاً به HTTPS یا localhost نیاز دارد.

### ۱۶.۵. تاریخ و عدد فارسی

datepicker جلالی برای فرم‌ها و نمایش زمان استفاده می‌شود. Traitها و helperهای PHP تبدیل تاریخ تولد و اعداد فارسی را انجام می‌دهند. در پایگاه داده، تاریخ باید تا حد امکان به قالب استاندارد قابل مقایسه ذخیره شود و جلالی‌بودن در ورودی و نمایش مدیریت گردد.

## ۱۷. گزارش‌گیری، PDF و Excel

### ۱۷.۱. گزارش مددجو

معماری گزارش مددجو از اجزای زیر تشکیل شده است:

- `BeneficiaryReportCriteria`: ساختار معیارها و نسخه آن‌ها؛
- `BeneficiaryReportFieldRegistry`: فهرست فیلدهای قابل فیلتر؛
- `BeneficiaryReportColumnRegistry`: ستون‌های قابل نمایش و export؛
- `BeneficiaryReportSemantics`: نگاشت مفاهیم زبان طبیعی و معنای دامنه؛
- `BeneficiaryReportQuery`: ساخت query نهایی؛
- `BeneficiaryOperationalFilters`: فیلترهای عملیاتی مانند سابقه خدمت یا فعالیت.

این ساختار نسبت به شرط‌های hard-coded در UI توسعه‌پذیرتر است.

### ۱۷.۲. گزارش‌های عملیاتی

گزارش‌های مجزا برای ورودی، تحویل و خروج توزیع وجود دارد. indexهای migration برای گیرنده، تاریخ، خدمت، پرونده و حضور به کارایی این گزارش‌ها کمک می‌کنند.

### ۱۷.۳. خروجی

Excel برای چندین دامنه با Laravel Excel تولید می‌شود. PDF گزارش مددجو از قالب Blade اختصاصی و DOMPDF استفاده می‌کند. هنگام انتقال سند یا گزارش فارسی به PDF باید embedding فونت و پشتیبانی RTL در محیط تولید آزموده شود.

## ۱۸. قابلیت‌های هوش مصنوعی

### ۱۸.۱. دامنه قابلیت‌ها

سه قابلیت اختیاری شناسایی شد:

1. تحلیل خلاصه پرونده مددجو و پیشنهاد اقدامات یا یادآوری؛
2. تبدیل درخواست زبان طبیعی کاربر به فیلتر ساخت‌یافته گزارش مددجویان؛
3. تولید پیش‌نویس پیام پیگیری برای SMS یا WhatsApp.

### ۱۸.۲. ساختار پیاده‌سازی

قراردادها در `app/Contracts/Ai` تعریف شده‌اند و پیاده‌سازی‌ها در `app/Services/Ai` قرار دارند. اتصال قرارداد به پیاده‌سازی در Provider انجام می‌شود. این طراحی امکان جایگزینی ارائه‌دهنده یا ساخت fake برای آزمون را فراهم می‌کند.

درخواست‌ها به endpoint `/responses` در base URL تنظیم‌شده ارسال می‌شوند. ویژگی‌های فنی مشاهده‌شده:

- مدل قابل تنظیم با `OPENAI_MODEL`؛
- `store=false` برای جلوگیری از درخواست ذخیره پاسخ در سرویس؛
- JSON Schema سخت‌گیرانه برای خروجی ساخت‌یافته؛
- timeout قابل تنظیم؛
- retry برای خطاهای موقت؛
- exception اختصاصی برای خطای قابل نمایش؛
- آزمون‌های واحد با fake کردن HTTP.

### ۱۸.۳. حفاظت از داده حساس

`SensitiveTextRedactor` و builderهای context پیش از ارسال داده، مواردی مانند نام، شناسه، موبایل، ایمیل، شماره شبا و داده‌های حساس را حذف یا ناشناس می‌کنند. این اقدام ضروری است، اما برای بهره‌برداری واقعی باید با سیاست حریم خصوصی سازمان، قرارداد پردازش داده و محل پردازش ارائه‌دهنده نیز تطبیق داده شود.

### ۱۸.۴. کنترل انسانی

پیام تولیدشده صرفاً پیش‌نویس است و ارسال خودکار از روی کد بررسی‌شده نتیجه‌گیری نمی‌شود. کاربر باید متن را بازبینی کند. این الگو برای جلوگیری از ارسال اطلاعات نادرست یا نامناسب مناسب است.

### ۱۸.۵. فعال‌سازی

در صورت خالی‌بودن `AI_CASE_ASSISTANT_API_KEY` قابلیت عملاً غیرفعال است. تنظیمات در `config/services.php` قرار دارند:

```env
AI_CASE_ASSISTANT_API_KEY=
OPENAI_MODEL=gpt-5-mini
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_TIMEOUT=45
```

نام مدل فقط مقدار پیکربندی پروژه است و دسترس‌پذیری واقعی آن نزد ارائه‌دهنده در زمان استقرار باید تأیید شود.

## ۱۹. مدیریت فایل، تصویر و پیوست

`OptimizedImageStorage` ذخیره و بهینه‌سازی تصویر را متمرکز می‌کند. مدارک و تصاویر مددجو، تصویر دسته خدمت و عکس پروفایل نمونه‌های فایل‌های سامانه‌اند.

پیوست‌های پرونده از کنترلر محافظت‌شده stream می‌شوند. برای استقرار باید این موارد روشن باشد:

- کدام فایل‌ها عمومی و کدام خصوصی‌اند؛
- حداکثر حجم و MIME مجاز؛
- سیاست نگهداری و حذف؛
- antivirus یا content scanning؛
- محل backup؛
- نحوه دسترسی در استقرار چندسروری.

پیکربندی فعلی `FILESYSTEM_DISK=local` است. این انتخاب برای یک سرور واحد ممکن است کافی باشد، اما برای scale افقی یا نیاز به بازیابی قوی، object storage سازگار با S3 مناسب‌تر است.

## ۲۰. نصب و راه‌اندازی محیط توسعه

### ۲۰.۱. پیش‌نیازها

- PHP 8.2 یا جدیدتر؛
- Composer؛
- Node.js و npm سازگار با Vite 6؛
- extensionهای PHP موردنیاز Laravel، پایگاه داده، فایل و تصویر؛
- SQLite برای راه‌اندازی ساده یا MySQL برای محیط مشابه تولید و آزمون؛
- دسترسی نوشتن به `storage` و `bootstrap/cache`.

### ۲۰.۲. نصب وابستگی‌ها

```powershell
composer install
npm install
```

### ۲۰.۳. ساخت فایل محیط

در PowerShell:

```powershell
Copy-Item .env.example .env
php artisan key:generate
```

### ۲۰.۴. راه‌اندازی SQLite

`.env.example` از SQLite استفاده می‌کند:

```env
DB_CONNECTION=sqlite
```

فایل پایگاه داده:

```powershell
New-Item -ItemType File database\database.sqlite -Force
```

سپس:

```powershell
php artisan migrate --seed
```

### ۲۰.۵. راه‌اندازی MySQL

برای MySQL باید مقادیر `DB_HOST`، `DB_PORT`، `DB_DATABASE`، `DB_USERNAME` و `DB_PASSWORD` در `.env` تنظیم شوند. پایگاه و کاربر باید از پیش با charset مناسب فارسی، ترجیحاً `utf8mb4`، ساخته شوند.

### ۲۰.۶. storage و asset

در صورت استفاده از disk عمومی:

```powershell
php artisan storage:link
```

اجرای توسعه:

```powershell
composer run dev
```

یا در دو ترمینال:

```powershell
php artisan serve
npm run dev
```

ساخت تولید:

```powershell
npm run build
```

### ۲۰.۷. قالب‌بندی و آزمون

```powershell
.\vendor\bin\pint
php artisan test
```

## ۲۱. تحلیل اسکریپت‌های Composer و npm

### ۲۱.۱. Composer

| اسکریپت | عملکرد |
|---|---|
| `post-autoload-dump` | کشف packageهای Laravel پس از ساخت autoload |
| `post-update-cmd` | انتشار assetهای Laravel پس از update |
| `post-root-package-install` | ایجاد `.env` از نمونه در صورت نبود فایل |
| `post-create-project-cmd` | تولید کلید، ساخت SQLite و اجرای migration |
| `composer run dev` | اجرای هم‌زمان Laravel server و Vite با `concurrently` |

اسکریپت `dev` از دستور `set FORCE_COLOR=true` استفاده می‌کند که با محیط Windows سازگار است؛ برای اجرای مستقیم در shellهای Unix ممکن است به بازنویسی نیاز داشته باشد.

### ۲۱.۲. npm

| اسکریپت | عملکرد | وضعیت |
|---|---|---|
| `npm run dev` | اجرای Vite dev server | معتبر |
| `npm run build` | ساخت asset تولید | معتبر |
| `npm start` | اجرای `node index.js` | نیازمند اصلاح؛ فایل مقصد مشاهده نشد |

### ۲۱.۳. entryهای Vite

سه entry تعریف شده است:

- `resources/sass/app.scss`
- `resources/css/app.css`
- `resources/js/app.js`

Bootstrap هم در زنجیره CSS و هم در SCSS وارد می‌شود. احتمال تکرار بخشی از CSS در bundle وجود دارد و باید با گزارش build و ابزار تحلیل bundle تأیید شود.

## ۲۲. پیکربندی محیط

### ۲۲.۱. پیکربندی نمونه

`.env.example` ویژگی‌های زیر را دارد:

- محیط local و debug فعال؛
- SQLite؛
- session، cache و queue مبتنی بر پایگاه داده؛
- mail روی log؛
- filesystem محلی؛
- OpenAI غیرفعال به دلیل کلید خالی؛
- locale و timezone پیش‌فرض `en` و `UTC`.

از آنجا که برنامه فارسی و تهران‌محور است، بهتر است فایل نمونه به `APP_LOCALE=fa` و `APP_TIMEZONE=Asia/Tehran` نزدیک شود تا راه‌اندازی توسعه با رفتار واقعی سازگار باشد.

### ۲۲.۲. پیکربندی تولید مشاهده‌شده

بدون بازنشر اسرار، فایل تولید موجود نشان می‌دهد:

- `APP_ENV=production`؛
- `APP_DEBUG=false`؛
- timezone تهران و locale فارسی؛
- MySQL؛
- session، cache و queue پایگاه‌داده‌ای؛
- cookie امن و `SameSite=lax`؛
- filesystem محلی؛
- mail روی log.

`MAIL_MAILER=log` به معنای ارسال‌نشدن ایمیل واقعی است و برای تولید فقط در صورتی صحیح است که سامانه عمداً ایمیل ارسال نمی‌کند. این موضوع **نیازمند تأیید عملیاتی** است.

### ۲۲.۳. reverse proxy

`bootstrap/app.php` مقدار `TRUSTED_PROXIES` را مستقیماً مصرف می‌کند. در فایل تولید بررسی‌شده این مقدار خالی بود، درحالی‌که آزمون پیکربندی تولید انتظار مقدار مشخصی مانند `REMOTE_ADDR` دارد. این ناسازگاری باید پیش از استقرار پشت Nginx، load balancer یا CDN حل شود؛ تنظیم نادرست می‌تواند تشخیص IP، HTTPS و URL تولیدشده را مختل کند.

### ۲۲.۴. cache، session و queue

هر سه سرویس می‌توانند از پایگاه داده استفاده کنند. migrationهای Laravel برای آن‌ها موجود است. در مقیاس بالاتر بهتر است Redis برای cache، session و queue ارزیابی شود تا بار عملیاتی از پایگاه تراکنشی جدا گردد.

## ۲۳. آزمون و تضمین کیفیت

### ۲۳.۱. ساختار آزمون

آزمون‌ها به دو گروه تقسیم شده‌اند:

- `tests/Feature`: مسیرها، Livewire، پایگاه داده و جریان‌های کاربر؛
- `tests/Unit`: منطق مستقل، criteria، redirect، AI و پیکربندی.

حوزه‌های پوشش‌داده‌شده شامل موارد زیر است:

- ورود، بازخورد احراز هویت و encoding صفحه؛
- redirect بر اساس مجوز؛
- مدیریت کاربر؛
- ثبت، ویرایش و جست‌وجوی مددجو و سرپرست؛
- گزارش پیشرفته و فیلتر AI؛
- پرونده مددجو و پیوست؛
- صف پرونده‌های ناقص؛
- همگام‌سازی زمینه خانواده؛
- موجودی و تمامیت دسته خدمت؛
- سهمیه اپراتور و مددکار؛
- ورودی، تحویل و خروج توزیع؛
- فیلدهای سفارشی ورودی؛
- چرخه فعالیت و اسکن QR؛
- ثبت حامی کودک؛
- تنظیمات تولید.

### ۲۳.۲. پیکربندی آزمون

`phpunit.xml` اتصال را با `force=true` روی MySQL محلی تنظیم می‌کند:

```text
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=avayeh_hamdeli_center
DB_USERNAME=root
```

بنابراین اجرای آزمون کامل برخلاف `.env.example` به SQLite متکی نیست و نیازمند MySQL محلی و پایگاه آماده است. بهتر است پایگاه آزمون نام مستقل و کاربر محدود داشته باشد تا خطر دستکاری داده توسعه کاهش یابد.

### ۲۳.۳. وضعیت اجرای آزمون در این تحلیل

در جریان تحلیل، اجرای ترکیبی برخی فرمان‌های Artisan مانند `about` و `route:list` در مهلت ۳۰ ثانیه پایان نیافت. مسیرها مستقیماً از فایل route بررسی شدند. suite کامل آزمون در این فرایند اجرا نشده است؛ بنابراین این سند درباره سبز بودن همه آزمون‌ها ادعای قطعی ندارد. اگر کندی bootstrap تکرار شود، profiling autoload، Providerها، اتصال پایگاه داده و cacheهای Laravel توصیه می‌شود.

### ۲۳.۴. پیشنهاد معیار کیفیت

- اجرای `php artisan test` در CI؛
- اجرای Pint؛
- ساخت production asset؛
- migration روی پایگاه خالی و نسخه ارتقایافته؛
- آزمون مرورگر برای دوربین و RTL؛
- آزمون هم‌زمانی توزیع با چند process؛
- بررسی امنیت dependencyها؛
- سنجش پوشش کد برای سرویس‌های بحرانی.

## ۲۴. امنیت، حریم خصوصی و نکات اجرایی

### ۲۴.۱. نقاط امنیتی مثبت

- middleware و Gateهای چندسطحی؛
- session-based authentication؛
- عدم ارائه پیوست حساس از URL مستقیم storage؛
- حذف نرم و ثبت دلیل؛
- audit log مددجو؛
- تراکنش و قفل در عملیات مالی/موجودی؛
- QR با توکن تصادفی، HMAC و وضعیت ابطال؛
- ثبت log اسکن؛
- anonymization پیش از AI؛
- `APP_DEBUG=false` و secure cookie در تولید؛
- حذف ستون تاریخی «رمز قابل مشاهده مدیر» در آخرین migration مورخ ۱۷ ژوئیه ۲۰۲۶.

اجرای migration آخر در همه محیط‌های تولید باید تأیید شود؛ صرف وجود migration به معنای اعمال‌شدن آن نیست.

### ۲۴.۲. حساب مدیر اولیه

`AdminUserSeeder` حساب مدیر اولیه را با رمز پیش‌فرض `admin123` ایجاد یا به‌روزرسانی می‌کند. این مقدار برای محیط واقعی ناامن است. راهکار پیشنهادی:

1. دریافت رمز از متغیر محیطی فقط هنگام bootstrap اولیه؛
2. اجبار تغییر رمز در نخستین ورود؛
3. جلوگیری از اجرای Seeder حساب پیش‌فرض در production؛
4. ثبت و ممیزی ایجاد حساب مدیر.

### ۲۴.۳. فایل‌های public

فایل‌هایی با ظاهر داده واقعی در `public/uploads` مشاهده شده‌اند. نگهداری فایل مددجو در مخزن Git یا پوشه عمومی می‌تواند نقض حریم خصوصی ایجاد کند. باید بررسی شود:

- آیا داده‌ها واقعی‌اند یا نمونه؛
- آیا در تاریخچه Git نیز وجود دارند؛
- آیا URL مستقیم دارند؛
- آیا حذف امن و چرخش backup لازم است.

### ۲۴.۴. داده‌های حساس

اطلاعات هویتی، سلامت، معلولیت، آسیب اجتماعی، بانک، تماس و محل سکونت در سامانه نگهداری می‌شود. حداقل کنترل‌های پیشنهادی:

- رمزنگاری در transit با TLS؛
- رمزنگاری backup و disk؛
- اصل کمترین دسترسی؛
- log دسترسی به پرونده و پیوست؛
- محدودیت export؛
- retention policy؛
- masking در محیط تست؛
- سیاست رخداد امنیتی و اطلاع‌رسانی؛
- بازبینی دوره‌ای حساب‌های غیرفعال.

### ۲۴.۵. اعتبارسنجی فایل

اعتبارسنجی MIME، اندازه و پسوند باید هم‌زمان انجام شود. نام فایل تولیدی نباید از نام ورودی کاربر مستقیماً مشتق شود. فایل‌های Office و PDF نیز می‌توانند محتوای مخرب داشته باشند و در محیط حساس بهتر است اسکن شوند.

### ۲۴.۶. AI و حریم خصوصی

ناشناس‌سازی موجود یک دفاع مناسب است، اما کافی‌بودن آن باید با نمونه داده واقعی آزموده شود. prompt و پاسخ نباید به‌طور کامل در logهای production ثبت شوند. همچنین باید مشخص باشد چه کاربرانی مجاز به استفاده از AI هستند.

### ۲۴.۷. عملیات و پشتیبان‌گیری

وجود queue، فایل محلی و داده حساس، نیازمند برنامه پشتیبان‌گیری از پایگاه داده و storage است. بازگردانی backup باید به‌صورت دوره‌ای تمرین شود. فقط داشتن فایل backup تضمین بازیابی نیست.

## ۲۵. مزایا و نقاط قوت پروژه

1. **پوشش دامنه گسترده:** سامانه فقط ثبت مددجو نیست و عملیات واقعی مرکز را از پرونده تا خدمت و فعالیت پوشش می‌دهد.
2. **یکپارچگی داده:** روابط خانوار، مددکار، خدمت، فعالیت و حامی در مدل واحد نگهداری می‌شوند.
3. **کنترل هم‌زمانی:** استفاده از تراکنش، `lockForUpdate` و قیود یکتا در توزیع و سهمیه از مهم‌ترین نقاط قوت فنی است.
4. **idempotency عملیاتی:** زنجیره ورودی تا خروج در برابر دوبارکلیک و اجرای موازی دفاع دارد.
5. **تفکیک Query، Service، Report و Export:** منطق پیچیده در چند حوزه از UI جدا شده است.
6. **حسابرسی و بازیابی:** حذف نرم، دلیل حذف، audit log و log اسکن QR قابلیت ردیابی را افزایش می‌دهد.
7. **کنترل دسترسی جزئی:** نقش و مجوز مستقل، تفکیک ایستگاه‌های توزیع را ممکن می‌سازد.
8. **گزارش‌پذیری:** فیلترهای ذخیره‌شده، criteria نسخه‌دار، Excel و PDF نیازهای مدیریتی را پوشش می‌دهند.
9. **بومی‌سازی:** RTL، فونت فارسی، تاریخ جلالی و ورودی مبلغ ریالی در طراحی لحاظ شده‌اند.
10. **QR عملیاتی:** صدور، ابطال، جایگزینی و تاریخچه اسکن در یک سرویس متمرکز است.
11. **AI با قرارداد و ناشناس‌سازی:** قابلیت اختیاری AI از کد دامنه جدا و دارای ساختار خروجی کنترل‌شده است.
12. **پوشش آزمون قابل توجه:** جریان‌های حساس توزیع، فعالیت، مجوز و گزارش دارای آزمون‌های اختصاصی‌اند.
13. **indexهای عملیاتی:** migrationهای جدید برای گزارش‌های پرتکرار indexهای هدفمند افزوده‌اند.
14. **حفاظت از حذف فیزیکی:** حذف قطعی برخی موجودیت‌های خدمت در production محدود شده است.

## ۲۶. محدودیت‌ها، چالش‌ها و نقاط قابل بهبود

### ۲۶.۱. مؤلفه‌های بسیار بزرگ

`CreatePerson`، `ServiceBatchCreator`، داشبورد مددکار، `User` و `Service` اندازه بالایی دارند. پیامدها:

- دشواری درک و تغییر؛
- افزایش احتمال side effect؛
- آزمون واحد سخت‌تر؛
- coupling میان UI، validation و persistence.

پیشنهاد می‌شود Form Object، Action، Policy و سرویس‌های کوچک‌تر استخراج شوند.

### ۲۶.۲. کتابخانه‌های کپی‌شده در `app/Helpers`

نسخه‌های حجیم Carbon، Assert و Morilog در کد برنامه نگهداری می‌شوند. این کار:

- تعداد فایل و زمان autoload را افزایش می‌دهد؛
- دریافت اصلاحات امنیتی را دشوار می‌کند؛
- مالکیت کد را مبهم می‌سازد؛
- احتمال ناسازگاری با نسخه Composer را بالا می‌برد.

در صورت نبود دلیل حقوقی یا فنی مشخص، این کتابخانه‌ها باید به dependency مدیریت‌شده تبدیل شوند.

### ۲۶.۳. فایل پشتیبان کد

`CreatePerson.php.bak` نباید در شاخه اصلی نگهداری شود.

### ۲۶.۴. ناسازگاری پیکربندی

- `.env.example` با locale و timezone واقعی سامانه همسو نیست.
- `TRUSTED_PROXIES` تولید با انتظار آزمون ناسازگار است.
- mail تولید روی log قرار دارد.
- filesystem تولید محلی است.
- آزمون‌ها به MySQL محلی و مشخص وابسته‌اند.

### ۲۶.۵. وابستگی‌های فرانت‌اند مبهم

بسته‌های `audit` و `openai` در فرانت‌اند و اسکریپت `npm start` باید پاک‌سازی یا مستند شوند.

### ۲۶.۶. احتمال تکرار CSS

ورود هم‌زمان Bootstrap از CSS و SCSS می‌تواند bundle را بزرگ و ترتیب cascade را پیچیده کند.

### ۲۶.۷. نبود API رسمی

برای اپلیکیشن موبایل، یکپارچه‌سازی بین‌سازمانی یا kiosk مستقل، API نسخه‌بندی‌شده وجود ندارد. افزودن API باید همراه با authentication token، rate limiting، resourceهای خروجی و مستند OpenAPI انجام شود.

### ۲۶.۸. تاریخچه تخصیص مددکار

بر اساس ساختار فعلی، گزارش‌ها عمدتاً تخصیص جاری سرپرست به مددکار را مبنا قرار می‌دهند. جدول روشن و مستقلی برای تاریخچه شروع و پایان تخصیص خانوار مشاهده نشد. برای گزارش تاریخی دقیق، مدل assignment تاریخ‌دار پیشنهاد می‌شود.

### ۲۶.۹. migrationهای پیچیده و وابسته به پایگاه

بعضی migrationها برای بازسازی ساختار خدمات و indexهای شرطی از منطق ویژه MySQL استفاده می‌کنند. اجرای `up` و `down` روی MySQL و SQLite باید در CI جداگانه آزموده شود.

### ۲۶.۱۰. داده و متن دو‌زبانه

برخی نام‌ها، پیام‌ها و کلیدهای داخلی انگلیسی و رابط فارسی است. این موضوع طبیعی است، اما پیام‌های کاربر و logها باید استاندارد شوند و رشته‌های رابط به فایل‌های ترجمه منتقل گردند.

### ۲۶.۱۱. کارایی bootstrap

timeout فرمان Artisan در تحلیل ممکن است محیطی باشد، اما به دلیل حجم helperها و Providerها نیازمند بررسی است. اندازه autoload classmap و اجرای کد در boot باید سنجیده شود.

### ۲۶.۱۲. داده‌های آپلودشده در مخزن

وجود فایل‌های واقعی‌نما در `public/uploads` یک ریسک فوری حریم خصوصی و حجم repository است و باید در اولویت بررسی قرار گیرد.

## ۲۷. موارد نیازمند تأیید

| موضوع | وضعیت مشاهده‌شده | پرسش موردنیاز |
|---|---|---|
| هدف دقیق سازمانی | از نام و دامنه کد استنتاج شده | آیا سامانه فقط برای یک مرکز است یا چند شعبه؟ |
| چندسازمانی بودن | tenant یا organization_id عمومی مشاهده نشد | آیا توسعه چندمرکزی برنامه‌ریزی شده است؟ |
| ارسال ایمیل | production روی log | آیا ایمیل عمداً غیرفعال است؟ |
| فضای فایل | local | آیا backup و اشتراک فایل میان سرورها تعریف شده است؟ |
| proxy | مقدار تولید خالی | زیرساخت پشت proxy/CDN است یا مستقیم؟ |
| داده `public/uploads` | فایل‌های واقعی‌نما وجود دارد | داده نمونه است یا اطلاعات واقعی؟ |
| `audit` در npm | مصرف مستقیم مشاهده نشد | چرا نصب شده است؟ |
| OpenAI JS SDK | مصرف مستقیم مشاهده نشد | آیا قابلیت فرانت‌اند حذف‌شده یا برنامه آینده است؟ |
| `npm start` | `index.js` مشاهده نشد | اسکریپت قدیمی است یا فایل جا افتاده؟ |
| تاریخچه مددکار خانوار | مدل مستقل مشاهده نشد | آیا گزارش تاریخی لازم است؟ |
| SMS/WhatsApp | فقط پیش‌نویس AI روشن است | ارسال واقعی از چه سامانه‌ای انجام می‌شود؟ |
| سیاست retention | در کد صریح نیست | سوابق و فایل‌ها چه مدت نگهداری می‌شوند؟ |
| سیاست backup | در مخزن صریح نیست | RPO و RTO مورد انتظار چیست؟ |
| استقرار آخرین migration | از کد قابل تشخیص نیست | آیا حذف ستون رمز قابل مشاهده روی production اعمال شده است؟ |

## ۲۸. پیشنهادهای توسعه آینده

### ۲۸.۱. اولویت فوری

1. حذف یا انتقال فایل‌های حساس از `public/uploads` و پاک‌سازی تاریخچه در صورت نیاز؛
2. حذف رمز پیش‌فرض Seeder و طراحی bootstrap امن مدیر؛
3. اعمال و تأیید آخرین migration امنیتی کاربران؛
4. اصلاح `TRUSTED_PROXIES`، mail و filesystem تولید؛
5. حذف فایل `.bak` و dependencyهای بدون استفاده؛
6. ایجاد CI برای آزمون، Pint، build و migration.

### ۲۸.۲. بهبود معماری

1. شکستن `ServiceBatchCreator` به چند Form/Action/Service؛
2. تفکیک مراحل `CreatePerson` به state objectها و actionهای دامنه؛
3. انتقال policyهای رکوردی از Provider و مدل به Laravel Policyها؛
4. کاهش مسئولیت مدل `User`؛
5. تعریف DTO برای داده‌های ثبت خدمت، تحویل و AI؛
6. استانداردسازی eventهای دامنه برای audit و notification.

### ۲۸.۳. بهبود داده

1. ایجاد جدول تاریخچه تخصیص خانوار به مددکار؛
2. تعریف data dictionary رسمی برای فیلدهای حساس؛
3. افزودن constraintهای پایگاه داده برای قواعدی که هنوز فقط در کد هستند؛
4. طراحی archive و retention؛
5. ایجاد داده آزمایشی ناشناس و factoryهای دامنه.

### ۲۸.۴. بهبود امنیت

1. فعال‌سازی MFA برای مدیران؛
2. rate limiting روی login و endpoint اسکن؛
3. log دسترسی و export پرونده؛
4. antivirus برای پیوست؛
5. مدیریت secret با secret manager؛
6. بازبینی dependency و SAST دوره‌ای؛
7. رمزنگاری ستون‌های بسیار حساس در صورت نیاز.

### ۲۸.۵. بهبود تجربه کاربری

1. استانداردکردن design system بین Tailwind و Bootstrap؛
2. کاهش asset تکراری؛
3. بهبود accessibility و ناوبری صفحه‌کلید؛
4. نمایش آفلاین یا صف موقت برای ایستگاه‌های توزیع با اینترنت ناپایدار؛
5. بهبود راهنمای خطا و recovery اسکنر؛
6. آزمون روی دستگاه‌های کم‌توان و مرورگرهای رایج کاربران.

### ۲۸.۶. توسعه API و یکپارچه‌سازی

در صورت نیاز سازمان:

- ایجاد `/api/v1`؛
- استفاده از Laravel Sanctum یا روش متناسب؛
- تعریف OpenAPI؛
- Resource و error envelope استاندارد؛
- webhook برای رخدادهای خدمت و فعالیت؛
- rate limit و audit مستقل.

### ۲۸.۷. عملیات و مشاهده‌پذیری

1. مانیتور خطا، زمان پاسخ و query کند؛
2. dashboard سلامت queue و failed jobs؛
3. alert برای پرشدن disk و شکست backup؛
4. correlation ID برای جریان‌های چندمرحله‌ای؛
5. سنجش زمان صفحات Livewire بزرگ؛
6. cache کردن گزارش‌های سنگین با invalidation مشخص.

## ۲۹. جمع‌بندی نهایی

سامانه مرکز آوای همدلی یک برنامه عملیاتی گسترده و نسبتاً بالغ بر بستر Laravel است که چند حوزه پیچیده سازمان حمایتی را در یک مدل داده مشترک گردآوری می‌کند. ارزش فنی اصلی آن در مدل‌سازی ارتباط مددجو، خانوار، مددکار، خدمت و فعالیت؛ کنترل دقیق تحویل؛ گزارش‌گیری چندبعدی؛ و استفاده از تراکنش و قیود پایگاه داده برای جلوگیری از ناسازگاری نهفته است.

پروژه از یک CRUD ساده فراتر رفته و در بخش‌های توزیع سه‌مرحله‌ای، سهمیه دسته‌محور، حضور فعالیت، QR، پرونده زمانی و گزارش نسخه‌دار منطق دامنه قابل توجهی دارد. وجود Service و Queryهای مستقل و آزمون‌های اختصاصی، پایه مناسبی برای توسعه آینده ایجاد کرده است.

در مقابل، چند کلاس بسیار بزرگ، dependencyهای کپی‌شده، پیکربندی‌های ناهمسو و ریسک فایل‌های عمومی هزینه نگهداری و امنیت را افزایش می‌دهند. مهم‌ترین اقدام بعدی، تثبیت امنیت و استقرار، پاک‌سازی مخزن و سپس refactor تدریجی مؤلفه‌های بزرگ همراه با آزمون regression است.

## ۳۰. خلاصه مدیریتی

سامانه مرکز آوای همدلی یک سامانه اطلاعاتی تحت وب برای مدیریت چرخه کامل حمایت از مددجویان است. این سامانه اطلاعات هویتی و خانوادگی را با عملیات مددکاری، خدمات حمایتی، توزیع اقلام، فعالیت‌ها، حضور، حامیان کودک و پرونده کارشناسی در یک بستر یکپارچه مرتبط می‌کند. کاربران بر اساس نقش و مجوز به پنل‌های مدیریت، مددکاری، توزیع، فعالیت یا حمایت کودک هدایت می‌شوند.

از نظر فنی، پروژه با PHP 8.2، Laravel 11، Livewire 3 و Eloquent پیاده‌سازی شده و رابط فارسی راست‌به‌چپ آن از Blade، Alpine.js، Vite، Tailwind و Bootstrap بهره می‌گیرد. پایگاه داده در توسعه می‌تواند SQLite و در تولید MySQL باشد. خروجی Excel و PDF، QR هویتی، اسکن دوربین و قابلیت اختیاری OpenAI نیز در سامانه پیش‌بینی شده است.

مهم‌ترین ارزش فنی پروژه، کنترل یکپارچگی عملیات حساس است. در توزیع خدمت، موجودی و سهمیه با تراکنش و قفل پایگاه داده کنترل می‌شوند و زنجیره ورودی، تحویل و خروج در برابر ثبت تکراری و اجرای هم‌زمان مقاوم شده است. فعالیت‌ها چرخه عمر مشخص، ظرفیت، تخصیص اپراتور و حضور یکتا دارند. پرونده مددجو نیز داده‌های پراکنده خدمت، فعالیت، یادداشت، فاکتور و پیگیری را در یک timeline گردآوری می‌کند.

پروژه دارای ساختار آزمون قابل توجه و تفکیک مناسب Service، Query، Report و Export در بخش‌های مهم است. بااین‌حال، برای آماده‌سازی بلندمدت و بهره‌برداری مطمئن باید چند موضوع در اولویت قرار گیرد: حذف رمز و فایل‌های حساس پیش‌فرض، تأیید migrationهای امنیتی، اصلاح پیکربندی تولید، انتقال فایل‌ها به storage امن، پاک‌سازی dependencyهای مبهم و شکستن مؤلفه‌های بسیار بزرگ به واحدهای کوچک‌تر.

در مجموع، سامانه از نظر پوشش دامنه و منطق عملیاتی ظرفیت تبدیل‌شدن به زیرساخت اصلی مرکز را دارد. با اجرای اقدامات امنیتی و refactor تدریجی، می‌توان قابلیت اعتماد، نگهداری‌پذیری، کارایی و آمادگی آن برای توسعه‌های آینده یا استفاده چندمرکزی را به‌طور معناداری افزایش داد.
